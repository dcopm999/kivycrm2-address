============
Installation
============

At the command line::

    $ easy_install kivycrm2-address

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv kivycrm2-address
    $ pip install kivycrm2-address
